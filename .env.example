# Environment Variables Configuration
# Note: The system runs locally with local LLMs (e.g., Ollama) or offline stylometry engines.
# No Gemini API keys or third-party cloud keys are required.

# PORT: (Optional) The HTTP port for the web server (Defaults to 3000)
PORT=3000
